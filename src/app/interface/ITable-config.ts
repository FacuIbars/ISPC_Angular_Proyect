export interface TableConfig {
    isPaginable?: boolean;
    showFilter?: boolean;
    showActions?: boolean;
    showSeeButton?: boolean;
    showEditButton?: boolean;
    showDeleteButton?: boolean;
  }